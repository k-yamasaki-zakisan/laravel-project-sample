<?php

namespace App\Http\Requests\Licenses;

use App\Http\Requests\RequestBase;

class SearchLicenseRequest extends RequestBase
{
/*
	protected $rules = [
		'search' => ['bail', 'nullable', 'array'],
		'search.employee_id' => ['bail', 'nullable'],
		'search.name' => ['bail', 'nullable'],
		'page' => ['bail', 'nullable', 'integer', 'min:1'],
	];
*/
}